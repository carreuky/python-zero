#default values, if called new_range(), the start and stop will automatically
# be used
def new_range( start =1, stop=2):
    return start+1, stop+1
    pass;


def knew_range( start, stop=0): # default values must come after
                               #key parameter never before
    pass;