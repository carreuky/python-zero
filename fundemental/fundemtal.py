

# tells you what the data is
type()
#emptyness or the absence think null
None

print(text='AlgoExpert is the best!')
#utlisezes terminal to asing a value 
#came be used in combatin with muples inputes
# and interperaltion or concatination

number = input('Number: ')
age = input('Age: ')
print('yo joe you number'+ number + 'makes sence you this' + age + 'years old')
#will prefor a divsion and returns the integer result
print(100 //6)

ord() #takes string returns ASCII nuymber
chr()# take ASCII  number return a letter

or # is like the || in javascript
not # this the ! in javascript
and # uses condiion that works when both arguments are True

list # its array in javascript

in # allow check if element is present in diectionary, list, set
    # if presetr returns True, if in list

append(lskjfsl) # add to end of list
                # it is like the push in javascript
                
len(list or str) # this is like the .length in javascript

pop() #same as pop( ) in javascript

list.count(element) # counts how many times element
                    #occursin in the list
                    
                    
list.index(element) # return idx of the first element
                    # postion in the list
        
list.remove(element) # remove first element in the list

array + array # two list create a new list of both values

'''
Used for string

'''

.count(el) # cunt how many times element apppears

.find(el) # indx( ) for list andindexOf in javascript

number.isdigit() #bool for number or not

string.split() #same as javascript.
 
string.replace(x,y) # reaplaces all the x elemetns with y
 
cocytous='frozen god'
s = f"helllo, {cocytous}"
# f string, this is terplation in javascript

print('string' * 5)# the string will repteate 5 times

seperator.join(list)#turn list to string


var = "algoexpert".upper().replace("o", "i").capitalize().lower().split("e")
print("foo".join(var) )


word = 'albedo can go one on one with shaliter'
print(word.split('s'))
print(word.count('a'))


#tuple can be like list nested
# you can use COUNT, IN, LEN, INDEX , a tuple is IMMUTABLE 
x =(1,2,3)
print(x.index(1))