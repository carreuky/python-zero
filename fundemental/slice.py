num = ['touchMe', 'Sebsatain', 'heroHero', 'yuri alpha', 'Aura']
    # orgin, stop & exclude, iterate by
    #range uses the same pinceple as above
#this is a clone
new_list = num[:]
reverse_clone = num[::-1]
print(reverse_clone)

#slice can be used to slice the string